"Act as an n8n Solutions Architect. I need you to modify the workflow named 'Main Workflow v4'.

1. Discovery: Search for the workflow named 'Main Workflow v4' to get its ID. Then, use n8n_get_workflow to download its current JSON structure. 2. Analysis: Analyze the JSON to identify the node where we should attach new logic. 3. Planning: "Think out loud" about how to construct the JSON for the new nodes required to add the logic of implementing the feature of uploading images to the AI agent for this request curl -X POST http://localhost:3000/quiz-link-request -H "Content-Type: application/json" -d '{"phone_number":"919385974117"}' . Modify the existing  . 4. Execution: Use n8n_update_partial_workflow to apply these changes. 5. Verification: Fetch the workflow again to confirm the nodes were added correctly"